# Server Config
PORT=3000

# Database
MONGODB_URI=mongodb://localhost:27017/tryonus

# Auth (Clerk)
CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# Storage (AWS S3)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_S3_BUCKET_NAME=try-on-us-assets

# Vertex AI
GCP_PROJECT_ID=...
GCP_LOCATION=us-central1

# Authentication (Important for non-GCP hosting)
# 1. Locally: Run 'gcloud auth application-default login'
# 2. Non-GCP Hosting (AWS, Heroku, etc.): 
#    - Create a Service Account in GCP Console (IAM & Admin -> Service Accounts)
#    - Download the JSON key file
#    - Set the path to the key file below:
# GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json